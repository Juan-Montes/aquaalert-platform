name: Deploy

on:
  push:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy placeholder - will be implemented in ci/github-actions branch
        run: echo "Deploy pipeline pending implementation"
